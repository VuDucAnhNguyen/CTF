## Linux commands
strings, cat, grep, find

## Exiftool
Sử dụng exiftool để xem metadata từ file
```
exiftool [file_name]
```

## Binwalk
Sử dụng binwalk để tính entropy
```
binwalk -E [file_name]
```
Sử dụng binwalk để trích xuất file ẩn
```
binwalk -e [file_name]
```